services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: jason
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"

volumes:
  postgres_data: